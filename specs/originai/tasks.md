# OriginAI Task List

- Add `CHANGELOG.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, and `LICENSE`.
- Add Docker support with `Dockerfile` and `.dockerignore`.
- Add GitLab CI coverage configuration and `pytest.ini`.
- Add route tests for home, analyzer, dashboard, investor report, and upload.
- Create Spec-Kit templates and feature spec docs.
- Verify repository compliance with docs and tests.
